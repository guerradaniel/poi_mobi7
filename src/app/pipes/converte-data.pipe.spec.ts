import { ConverteDataPipe } from './converte-data.pipe';

describe('ConverteDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverteDataPipe();
    expect(pipe).toBeTruthy();
  });
});
